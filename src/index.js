import React from "react";
import ReactDOM from "react-dom/client";
import "./index.css";
import LearningMELPlatform from "./LearningMELPlatform";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
  <React.StrictMode>
    <LearningMELPlatform />
  </React.StrictMode>
);